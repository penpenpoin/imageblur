#include <vector>

std::vector<int> Blur(const std::vector<std::vector<int>> image,int M){
    std::vector<std::vector<int>> after_blur(image.size(),std::vector<int>(image[0].size()));

    for(int i =0 ;  i < image.size(); i += M){
        for(int j = 0; j< image[i].size() ; j += M){
            x
        }
    }
}
